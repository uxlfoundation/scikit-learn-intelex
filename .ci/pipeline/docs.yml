#===============================================================================
# Copyright 2022 Intel Corporation
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#===============================================================================

trigger:
  branches:
    include:
    - "*"
  paths:
    include:
    - requirements-doc.txt
    - doc/
    - .ci/pipeline/docs.yml
    - .github/Pull_Request_template.md

pr:
  branches:
    include:
    - main
    - rls/*
  paths:
    include:
    - requirements-doc.txt
    - doc/
    - .ci/pipeline/docs.yml
    - .github/Pull_Request_template.md

variables:
  - name: 'PYTHON'
    value: python

jobs:
- job: Docs
  pool:
    vmImage: 'ubuntu-22.04'
  steps:
  - script: |
      bash .ci/scripts/describe_system.sh
    displayName: 'System info'
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.11'
      addToPath: true
  - script: sudo apt-get update && sudo apt-get install -y clang-format
    displayName: 'apt-get'
  - script: |
      pip install daal-devel impi-devel
      pip install -r dependencies-dev
      pip install -r requirements-doc.txt
      pip install -r requirements-test.txt
      pip list
    displayName: 'Install requirements'
  - script: |
      echo "Starting Build daal4py/sklearnex"
      echo "Current directory1: $(pwd)"
      export PREFIX=$(dirname $(dirname $(which python)))
      export e=$PREFIX
      ./conda-recipe/build.sh
      echo "Python executable being used1: $(which python)"
      echo "Current directory2: $(pwd)"
      echo "PREFIX is set to1: $PREFIX"
      echo "Build daal4py/sklearnex completed successfully"
    displayName: 'Build daal4py/sklearnex'
  - script: |
      echo "Starting Build daal4py documentation"
      echo "Current directory3: $(pwd)"
      export LD_LIBRARY_PATH=$(dirname $(dirname $(which python)))/lib:$LD_LIBRARY_PATH
      echo "LD_LIBRARY_PATH is set to: $LD_LIBRARY_PATH"
      cd doc/daal4py
      echo "Current directory: $(pwd)"
      echo "PREFIX is set to2: $PREFIX"
      python -c "import sys; print('Current sys.path1:', sys.path)"
      echo "Python executable being used2: $(which python)"
      make html
      echo "Build daal4py documentation completed successfully"
    displayName: 'Build daal4py documentation'
  - script: |
      echo "Starting Build scikit-learn-intelex documentation"
      echo "Current directory: $(pwd)"
      cd doc
      python -c "import sys; print('Current sys.path2:', sys.path)"
      echo "Python executable being used3: $(which python)"
      make html
      echo "Build scikit-learn-intelex documentation completed successfully"
    displayName: 'Build scikit-learn-intelex documentation'
  - script: |
      mkdir $(Build.ArtifactStagingDirectory)/html/daal4py
      mkdir $(Build.ArtifactStagingDirectory)/html/sklearnex
      cp -R doc/daal4py/_build $(Build.ArtifactStagingDirectory)/html_daal4py
      cp -R doc/_build $(Build.ArtifactStagingDirectory)/html_sklearnex
    displayName: 'Copy build'
  - task: PublishPipelineArtifact@0
    inputs:
      artifactName: 'documentation'
      targetPath: '$(Build.ArtifactStagingDirectory)/'
